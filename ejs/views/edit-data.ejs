<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Task Data</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f5f5f5;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
        }
    
        h1 {
            color: #333;
            margin-top: 20px;
        }
    
        form {
            display: flex;
            flex-direction: column;
            margin-top: 20px;
        }
    
        input[type="text"] {
            padding: 10px;
            margin-bottom: 10px;
            border: 1px solid #ccc;
            border-radius: 5px;
        }
    
        button[type="submit"] {
            background-color: #007bff;
            color: white;
            padding: 10px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }
    
        button[type="submit"]:hover {
            background-color: #0056b3;
        }
        a {
            text-decoration: none;
            color: #007bff;
            margin-top: 50px;
        }

        a:hover {
            text-decoration: underline;
        }
    </style>
    
</head>
<body>
    <h1>Edit Task Data</h1>
    <!-- data edit here -->
    <form action="/update-data/<%= task._id %>" method="POST">
        <input type="text" name="taskName" value="<%= task.name %>" required>
        <input type="text" name="taskDescription" value="<%= task.description %>" required>
        <button type="submit">Save Changes</button>
    </form>
    <a href="/">Back to Home</a>    
</body>
</html>




