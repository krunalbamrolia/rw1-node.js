<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Delete Book</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            margin: 50px;
        }
        h1 {
            margin-bottom: 20px;
        }
        p {
            margin: 5px 0;
        }
        .button {
            display: inline-block;
            margin: 10px;
            padding: 10px 20px;
            font-size: 16px;
            cursor: pointer;
            text-align: center;
            text-decoration: none;
            outline: none;
            color: #fff;
            background-color: #007bff;
            border: none;
            border-radius: 5px;
            transition: background-color 0.3s;
        }
        .button:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>
    <h1>Delete Book</h1>
    <p>Are you sure you want to delete this book?</p>
    <p>Title: <%= book.title %></p>
    <p>Author: <%= book.author %></p>
    <p>Price: <%= book.page %></p>
    
    <form id="deleteForm" method="POST" action="/delete/<%= task.id %>">
        <input type="hidden" name="bookId" value="<%= book._id %>">
        <button class="button" type="button" onclick="confirmDelete()">Confirm Delete</button>
    </form>
    
    <a class="button" href="/add">Cancel</a>
</body>
</html>
